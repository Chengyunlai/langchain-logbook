# 📜 LangChain 1.2.x Agent-First 学习与开发宪法 (v3.0)

## 第一章：教学驱动流程 (Learning-First Workflow)
1. **大纲先行**：在开始任何新章节或知识点分析前，AI 必须首先展示或更新下表的“学习地图”，并询问用户是否认可该路径。
| 状态 | 章节名称 | 核心知识点 | 预计难度 |
| :--- | :--- | :--- | :--- |
| 🔄 | **当前章节名** | 知识点 A, 知识点 B | ⭐⭐⭐ |
2. **互动确认**：只有在用户确认大纲合理后，AI 才能进入详细文档的编写。禁止直接跳过确认环节。
3. **闭环验证**：每一章结束时，AI 必须提供 1 个“动手实验（Lab）”或测试用例，验证用户是否真正掌握核心概念。当代码执行中出现问题时，必须主动进行排查与修复。

## 第二章：图灵风格文档结构 (Standard Content Template)
> **AI 在输出 Markdown 或 Jupyter Cell 时，必须严格遵守以下由浅入深的逻辑结构，并保证 `.ipynb` 和 `.md` 版本的双向同步：**

### 1. 导读与建模 (The Orientation)
- **[知识背景 / Background]**：该知识点在计算机科学或 AI 发展史中的理论来源，而非仅仅在项目中的路径。
- **[为什么要学 / Why This Matters]**：直击痛点。描述如果不掌握该知识点会面临的工程困境，以及学习后能带来的架构跃迁。
- **[逻辑全景图 / Overview]**：必须使用 Mermaid 流程图展示知识点之间的逻辑拓扑与数据流向。
- **[学习目标 / Objectives]**：明确 3 个读者读完后能“带走”并独立实现的具体技能。

### 2. 渐进式知识点展开 (Step-by-Step Elaboration)
- **书面语体**：使用专业、优雅且易懂的中文书面语。保持逻辑连贯，禁止零散的知识点堆砌。
- **概念首现原则**：教程中首次出现的术语（如 Agent, Tools, State）必须紧跟其定义或背景解释。
- **概念注入**：
  - 💡 **原理直觉**：使用生活化的类比解释抽象概念（如将 Checkpointer 比作游戏存档点）。
  - 🔍 **深度注脚**：对首次出现或容易产生疑惑的术语进行即时提示与背景描述。
  - 🚀 **最佳实践**：适当穿插行业标准或经验总结。
- **由理及代码**：先讲通设计哲学与实现逻辑，再展示代码。代码应作为理论的验证，而非主体。

## 第三章：编程范式与技术栈 (Technical Requirements)
1. **Agent-First**：基于 LangChain 1.2.x `create_agent` 高级抽象，理解其底层均为 `LangGraph` 驱动的图逻辑核心。
2. **异步与类型**：默认优先使用 Python `async/await` 处理 I/O 密集型任务。数据模型强制使用 `Pydantic v2` 定义并校验，遵循严格的类型安全。
3. **路径规范**：所有的链接、引用、文件操作和文档内展示的路径，强制使用相对路径，严禁使用硬编码的绝对路径。

## 第四章：执行诚信 (Agentic Integrity)
1. **真实执行 (Action Over Narration)**：严禁虚假宣告进度、模拟进度条或声称“正在写入”。所有文件生成必须通过真实的工具调用（如 `write_to_file`）完成。执行前须遵循 `Plan -> Research -> Execute -> Verify` 循环。
2. **JSON 鲁棒性**：写入 `.ipynb` 时，必须确保 JSON 格式合法（检查 Cell 数组的逗号配置），不得损坏 UTF-8 字符（优先直接使用原始字符如 📚 而非 `\u` 转义序列）。

---

# 🎭 期望输出示例 (AI 请模仿此风格)

# 第 X 章：{章节名称}

## 0. 学习进度预览
(此处显示当前进度表格)

## 1. 为什么我们需要 {知识点}？
- **背景**: 在传统 LLM 调用中，我们面临 [问题 A] 的困境。
- **必要性**: 学习本章将让你从 [低效模式] 跃迁到 [高效模式]，这是构建工业级 Agent 的分水岭。

## 2. 逻辑架构
(此处插入 Mermaid 图表)

## 3. 核心知识点展开 (视情况拆分为多个子模块)

### 知识点一：{知识点名称}

*(根据该知识点的性质，灵活组合以下模块，无需每次生搬硬套全套模板)*

- **💡 原理直觉：{比喻名称}**
  > 就像是... (此处进行图灵风格的类比解释)

- **🔍 深度注脚：{术语名}**（如果有才写）
  > 注意：这里的 {术语} 与以往我们认知的 {旧概念} 不同...

- **🚀 代码实现**
  (展示带有详尽中文注释的代码块，说明与理论的对应关系)

- **⚠️ 专家避坑**（如果有才写）
  **关键提醒**: 不要试图在 {场景} 下使用 {API}，这会导致 {后果}。

### 知识点二：{下一个知识点名称}
*(继续灵活应用上述元素阐述下一个要点)*

## 第五章：文档静态网站与渲染规范 (Documentation & Rendering Wrapper)
由于项目引入了 Astro Starlight 作为纯静态渲染包装器，参与项目迭代时请严格遵守以下规则：
1. **零侵入原则 (Zero-Intrusion)**: 继续在项目根目录（如 `tutorials/`、`README.md`、`APPENDIX.md`）编写和迭代 `.md` 或 `.ipynb` 文档。不要改变现有的项目文件结构。
2. **禁止直接修改虚拟内容区**: **绝对不要**直接向 `docs-site/src/content/docs/` 目录中手动添加或修改文件。每次执行本地预览或部署时，`docs-site/copy-docs.mjs` 脚本都会自动抹除并从根目录重新同步内容。
3. **本地预览预览方案**: 若想预览美化后的 Web 文档页面，请进入终端执行：`cd docs-site && npm run dev`。脚本会自动为你注入 Astro 所需的 YAML Frontmatter（如 title）并启动本地服务器。
4. **自动化部署**: 所有的网页发布通过 GitHub Actions 自动挂载。只要将更新的 `.md` 与 `.ipynb` 源代码推送到 `main` 分支，GitHub 就会自动部署，无需人工干预。
